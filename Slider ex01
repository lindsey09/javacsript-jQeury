<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slider Example</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <style>
        .slider{width:400px; height:400px; border:5px solid #000; overflow: hidden; margin:100px auto;}
        .slider ul{width:300%; height:100%; display:flex; flex-wrap:wrap; padding:0; margin:0; list-style: none; position:relative; tarnsition:1s; left:0;}
        /*transition에 left값이 있는 경우에는 처음엔 auto에서 숫자로 바뀌어서 transition만 넣으면 제일 처음것은 동작이 안됨. left값을 0으로 설정해주어야 숫자에서 숫자로 
        바뀌는것이기 때문에 스스륵 움직일 수 있음*/
        .slider ul li{flex: 1; height:100%;}
        .slider ul li:nth-child(1){background:#f00;}
        .slider ul li:nth-child(2){background:#c7c;}
        .slider ul li:nth-child(3){background:#fc0;}
        
    </style>
    <script>
        $(document).ready(function(){
            $('.next').click(function(){
                // ture에 !표 붙으면 반대연산자 false가 됨. !는 반대연산자가 됨.
                let left = parseInt( $('.slider ul').css('left')) //전체 ul의 너비를 왼쪽으로 움직이는 것, 그러기 위해서는 왼쪽의 현재 위치값을 알아야 함.
                //parseInt 정수로 바꿔주는 함수, 현재값을 -400px로 문자열로 인식하기 때문에 숫자로 변경해주어야 함.
                //console.log(left);
                if (left==-800){
                   // $('.slider ul').css('left', 0);
                   return; //retufn을 만나면 여기서 종료
                }  //원동을 멈춘다. 
            
                console.log(left);
                $('.slider ul').css('left', left-400);
                //다시 처름으로 돌아가는 것 해당 위치로
                
            });


            $('.prev').click(function(){
                let right = parseInt( $('.slider ul').css('left'))

                console.log(right);
                $('.slider ul').css('left', right+400); //position으로 움직이는것은 같은 값을 가지고 움직여야 됨, 왼쪽으로 갔다가 오른쪽으로 가는것은 이해할 수 없으니까 그래서 같은값인 left를 불러옴
                   
                if (right==0){
                    $('.slider ul').css('left',-800);
                } //원레 위치로 되돌아가기

                
            });

        });

        
    </script>
</head>
<body>
    <div class="slider">
        <ul>
            <li></li>
            <li></li>
            <li></li>
        </ul>
    </div>
    <button class="prev">PREV</button>
    <button class="next">NEXT</button>


</body>
</html>
